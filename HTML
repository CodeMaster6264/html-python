{% load static %}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link rel="stylesheet" href="{% static 'css/dashboard.css' %}">
</head>
<body>

    <!------------------------Main------------------------>
    <div class="main">
  
        <div class="welcome">
            <h2>Welcome, {{ user.username }} to your Dashboard</h2>
        </div>

        <h2>My Bookings</h2>

        <div class="card">
            {% if bookings %}
                <table>
                    <thead>
                        <tr>
                            <th>Full Name</th>
                            <th>Email</th>
                            <th>Service</th>
                            <th>Date</th>
                            <th>Time</th>
                            <th>Description</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for booking in bookings %}
                        <tr>
                            <td>{{ booking.fullname }}</td>
                            <td>{{ booking.email }}</td>
                            <td>{{ booking.service }}</td>
                            <td>{{ booking.date }}</td>
                            <td>{{ booking.time }}</td>
                            <td>{{ booking.description }}</td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            {% else %}
                <p class="no-bookings">No bookings found.</p>
            {% endif %}
        </div>

        <div class="button-container">
            <a href="{% url 'booking' %}" class="btn"> Make a New Booking</a>
        </div>
    </div>

</body>
</html>
